"""Entry point for running the GUI via ``python -m advanced_codebase_analyzer``."""
from .gui.main_window import launch


if __name__ == "__main__":  # pragma: no cover - manual invocation only
    launch()
